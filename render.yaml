services:
  - type: web
    name: ict-306-rainfall
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn rainfall_prediction:app --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.13